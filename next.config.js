/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/emergency_fund_calc',  // Your repo name
  images: {
    unoptimized: true,
  }
}

module.exports = nextConfig
